// this is just a pseudo code at the moment

/**
 * Suppose we are going to write our own parser to understand the COBOL syntax
 * @param {string} str input
 * @return {*} TBC
 */
function ast(str) {

  return str
}


module.exports = { ast }
